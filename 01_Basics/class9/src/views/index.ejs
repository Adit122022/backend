<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Home Page</title>
  <!-- Tailwind CSS CDN -->
  <script src="https://cdn.tailwindcss.com"></script>

  <!-- Custom Theme Styles -->
  <style>
    /* Neon Theme (default) */
    .neon-theme {
      background-color: #000000;  /* Black */
      color: #22c55e;             /* Tailwind green-500 */
    }
    .neon-theme header,
    .neon-theme footer {
      background-color: #000000ce;
    }

    /* Dark Theme */
    .dark-theme {
      background-color: #111827; /* Tailwind gray-900 */
      color: #f3f4f6;            /* Tailwind gray-100 */
    }
    .dark-theme header,
    .dark-theme footer {
      background-color: #1f2937; /* Tailwind gray-800 */
    }

    /* Navigation Links */
    .nav-link {
      border: 1px solid currentColor;
      padding: 0.5rem 1rem;
      margin-left: 0.5rem;
      transition: background-color 0.3s, color 0.3s;
    }
    .nav-link:hover {
      background-color: black;
      color: #fff;
    }
  </style>
</head>
<body class="neon-theme min-h-screen flex flex-col">

  <!-- Header / Navigation -->
  <header class="py-4 px-8 flex items-center justify-between">
    <div class="text-xl font-bold">AuthPro</div>
    <nav>
      <a href="/users/register" class="nav-link rounded">Create Account</a>
      <a href="/users/login" class="nav-link rounded">Login</a>
      <!-- Theme Toggle Button -->
      <button class="nav-link rounded" onclick="toggleTheme()">Toggle Theme</button>
    </nav>
  </header>

  <!-- Main Content -->
  <main class="flex-grow flex flex-col items-center justify-center text-center px-4">
    <h1 class="text-3xl md:text-4xl font-bold mb-4">
      Welcome to the Authentication Project
    </h1>
    <p class="max-w-xl mb-2">
      A secure authentication system built with Node.js, Express, MongoDB, and JWT.
    </p>
    <p class="max-w-xl">
      This project includes user registration, login, and authentication 
      using modern security standards. Built for developers who want 
      a reliable and scalable auth system for their applications.
    </p>
    <a class="text-blue-500" href="/feed"> FEED </a>
  </main>

  <!-- Footer -->
  <footer class="text-center py-4">
    &copy; <%= new Date().getFullYear() %> My Website. All rights reserved.
  </footer>

  <!-- Theme Toggle Script -->
  <script>
    function toggleTheme() {
      const body = document.body;
      if (body.classList.contains('neon-theme')) {
        body.classList.remove('neon-theme');
        body.classList.add('dark-theme');
      } else {
        body.classList.remove('dark-theme');
        body.classList.add('neon-theme');
      }
    }
  </script>
</body>
</html>
